name: paginated_items_builder_demo
description: "Demo app for the package paginated_items_builder"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.11.0
  provider: ^6.1.5+1
  easy_container: ^1.0.8
  paginated_items_builder:
    path: "../"

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
