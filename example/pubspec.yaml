name: paginated_items_builder_demo
description: "Demo app for the package paginated_items_builder"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.7.0
  provider: ^6.1.2
  easy_container: ^1.0.5
  paginated_items_builder:
    path: "../"

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
