name: paginated_items_builder
description: Easier to display items in a list/grid view from your controllers directly or handling state internally. Supports pagination as well.
version: 0.0.1
homepage: https://github.com/rithik-dev/paginated_items_builder

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^1.0.4

flutter:
